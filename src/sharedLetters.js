/**
 * 多个单词组成的数组，找出所有单词都有的字符（可重复，顺序无关）
 * 返回这些重复字符组成的字符串
 */
const sharedLetters = (letterArr) => {

}

const letterArr = ['smooth', 'common', 'mooring']
/**
 * 三个单词都有'm'，并且都有两个'o'，返回'moo'（'omo', 'oom'也可）
 */
const result = sharedLetters(letterArr)
console.log('result:', result)
